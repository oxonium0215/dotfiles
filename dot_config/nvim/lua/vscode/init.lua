-- VSCode Neovim configuration
-- Minimal configuration for use within VSCode

-- Load core keymaps (subset that work in VSCode)
require("vscode.keymaps")

-- Load VSCode-specific plugins
require("vscode.plugins")